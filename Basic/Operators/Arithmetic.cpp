#include<iostream>
using namespace std;
int main(){
    int a=5, b=10;

    cout<<"sum = "<<(a+b)<<endl;
    cout<<"subtract = "<<(a-b)<<endl;
    cout<<"product = "<<(a*b)<<endl;
    cout<<"divide = "<<(a/b)<<endl;
    cout<<"modulo = "<<(a%b)<<endl;
    return 0;
}